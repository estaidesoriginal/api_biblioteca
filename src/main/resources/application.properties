spring.application.name=api-biblioteca

# Configuración de Base de Datos
spring.datasource.url=jdbc:postgresql://[db.cbsgbkcyylnjvtyoqozl.supabase.co]:6543/postgres?sslmode=require
spring.datasource.username=[postgres]
spring.datasource.password=[arApp.2025]
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
# update: crea/actualiza tablas automáticamente. Cambiar a 'validate' en producción.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

server.port=${PORT:8080}